//Jack Hosier, Java learning Variables Part 2. 

class Quadratic { 
  public static void main ( String[] args ) { 
    double x = 4.0;
    
    double equation = (3 * Math.pow(x, 2)) - (8 * x) + 4;
    
    System.out.println(equation);
  }
}
